# BMW from Korea — пошук і пригін BMW X5

## Що це за проєкт
**Особистий** (не робочий) проєкт Володимира: підбір вживаного **BMW X5 (G05)** у Кореї (Encar) для **пригону в Україну**, з розрахунком «під ключ», перевіркою історії та комплектації за VIN. Мета — вести список кандидатів, порівнювати їх і врешті **опублікувати сайт (GitHub Pages)**, щоб показувати іншим.

Репозиторій: `git@github.com:volodymyr-babak/bmw-from-korea.git` → Pages буде на `https://volodymyr-babak.github.io/bmw-from-korea/`.

> Історія: спочатку робилося як опублікований Claude Artifact `ff1c66c5-4e4c-4c7f-b9a6-672c48e4bbbd` («Шортлист X5 G05 з Кореї»). Тепер переносимо в цей репозиторій як повноцінний сайт.

---

## Критерії пошуку (фінальні, чинні на 2026-09-02)
- **Модель:** BMW **X5 (G05)** і **X6 (G06)** — X6 додано 2026-09-02.
- **Рік виготовлення (연식, не модельний):** 2019–2022.
- **Пробіг:** ≤ 110 000 км.
- **Комплектація:** M Sport (M 스포츠) / M50.
- **Двигун: ЛИШЕ дизель 30d.** Виключено: **бензин 40i**, **усі 45e (PHEV)**, **дизель 40d** (є в X6: `xDrive40d M 스포츠`), M50i, M50d. *(Рішення про дизель-онлі — TCO і профіль їзди, див. нижче.)*
- **Поріг ДТП:** ≤ 5 млн ₩ виплат за **власний** ремонт (`myAccidentCost`). Без списання/потопу (`totalLoss`/`flood`=0).
- **Ціна під ключ:** ≤ **$70 000** (стелю піднято з $65k → $70k 2026-09-01).
- **Салон:** лишаємо **тільки чорний і кавовий (Vernasca Coffee)**. Виключено: **рудий/cognac**, **світлий айворі (Ivory White)** і **червоний Tacora Red** (виключення червоного додано для X6 2026-09-02, діє й на X5).
- **✅ `contents.text` у detail API — текст оголошення. Найдешевше джерело правди.**
  Там регулярно є те, чого немає ні в API, ні в білд-листі: колір салону, кількість ключів,
  залишок протектора, продовжена гарантія, ціна нової, прокатна історія, визнані кузовні
  роботи, іноді **VIN** (`차대번호`). Сирий текст зберігається в `data/cars/<id>.json →
  sellerText`, тож перечитати його можна без запитів до Encar.
  Модулі: `tools/trim.py` (колір оббивки), `tools/seller.py` (VIN + високоточні факти).
- **✅ Колір салону з опису оголошення.**
  Окремого поля кольору оббивки в Encar немає, але продавці регулярно пишуть його
  текстом (`【실내/시트】…꼬냑 시트…` = cognac). Парсер — `tools/trim.py::seat_colour()`,
  підключений у `reject_reason()` **до** декодування, тож cognac / айворі / червоний
  відсіюються без витрати квоти mdecoder. Перевірено на двох авто з відомим VIN
  (`42445415`, `41334762`): опис казав «블랙시트», білд-лист — Vernasca Black, 2/2.
- **⚠️ Салон: окремого поля кольору оббивки в Encar API немає.** Але **Tacora Red і Ivory White видно на фото оголошення** — їх відсіюємо одразу, не витрачаючи дефіцитну спробу mdecoder (зроблено 2026-09-02: −9 червоних, −1 айворі). **⛔⛔ Cognac від кавового не відрізняють НІ фото, НІ опис.** Доведено двічі, і в різні боки: `42092261` на фото «рудий», а за VIN Coffee (лишили); `42244757` продавець описав як «브라운시트» (коричневий), а за VIN виявився **Vernasca Cognac `MCRI`** — прибрали. Тому в `tools/trim.py` слова розділені: `커피/모카/초콜릿` = явна кава (ключ `coffee`), а `브라운/갈색` = просто «коричневий» (ключ `brown`), і в UI це показується як «кава або cognac». Єдиний арбітр для цієї пари — білд-лист за VIN.
- **VIN є не завжди.** Encar показує його не для всіх лотів (напр. `42660021` — немає ні в
  detail, ні в inspection API). `check_existing()` у `watch.py` щогодини **підхоплює VIN,
  який з'явився пізніше**, і пише про це в звіт окремим блоком; наявний VIN ніколи не
  перетирається `None`.
- **Порядок перевірки салону** (від найдешевшого до найдорожчого): `contents.text`
  → фото → mdecoder за VIN. Опис і фото безкоштовні й миттєві, квота mdecoder — ні.
- **⚠️ Полярність в описі автоматом не розбирається.** `판금 하나 있습니다` = ремонт **є**,
  `무판금` = немає; `리스크` = «ризик», а не лізинг. Тому `seller.facts()` тримає лише
  однозначні шаблони (ключі, протектор, ціна нової, продовжена гарантія, «렌트 이력 무»,
  BPS, некурящий), а решта — курується руками з `sellerText`.
- **⚠️ VIN з тексту брати лише помічений `차대번호` + збіг коду року** (10-й знак:
  K=2019, L=2020, M=2021, N=2022). Дилери копіюють описи між оголошеннями: у `41142979`
  у кінці тексту лежав VIN зовсім іншого авто (2021 замість 2022). Так знайдено VIN для
  `42203413`, якого немає в API.
- **Метод перевірки салону за фото.** Завантажити 4 перші `photos.inner` (480×270), порахувати **медіанний відтінок** насичених пікселів (`s>0.35, v>0.2`): **0–7° = Tacora Red**, **14–36° = cognac/кава**, розрив між кластерами чистий. Айворі метрика відтінку не ловить (світлий і малонасичений) — для нього частка пікселів `v>0.62, 0.06<s<0.32, hue 20–70°`; але вона шумить від неба й стін на фото, тож **обов'язково дивитись очима у великому розмірі**. Правило: **де сумнів — авто лишаємо**.
- **⛔ Cognac від Coffee за фото не відрізнити — доведено на практиці.** `42660021`
  дав hue 25° проти 22° у підтвердженого Coffee `42092261`, тобто «в межах Coffee» —
  а продавець в описі написав `꼬냑 시트`, cognac. **Метрику відтінку використовувати
  тільки для червоного та айворі**, для пари cognac/кава — лише опис або VIN.
- **Cognac vs Coffee за фото — рахувати сидіння, а не весь кадр.** Медіана по кадру бреше:
  на фото «салону» багато кузова, у `42660021` вона дала 220° (синій кузов). Треба **кроп
  центрально-нижньої частини** (`x 25–85%`, `y 45–100%`) і фільтр `s>0.20, 0.18<v<0.92`.
  Еталони за VIN: `42092261` Coffee → hue 22°, sat 0,31, val 0,57; `41437048` Coffee →
  16°/0,30/0,64. Кандидат `42660021` дав **25°/0,32/0,56** — у межах Coffee, тож лишений
  із нотаткою в індексі до появи VIN. Cognac-еталона за VIN у нас поки немає.
- **Виключено також:** лізинг (`SellType`=='리스').
- **Прокатна / таксі / комерційна історія — стоп-фактор** (рішення 2026-09-02). Видно
  ЛИШЕ у звіті інспекції (`usageChangeTypes`); в оголошенні й у решті API цього немає.
  Перевірка в `reject_reason()` (`BAD_USAGE` у `watch.py`), плюс запасний фільтр за VIN
  у `watch-state.usageRejected` — на випадок, коли звіту немає.
- Не розбивати на «чисті / з ДТП» — один список, сортування за ціною під ключ.

---

## Розмитнення (ТОЧНА формула, звірена з 8 скріншотами carspy.io/Encar)
Мита рахуються від **фіксованої мінімальної митної вартості P (USD) за рік виготовлення**, а НЕ від корейської ціни. **Митниця рахує по P завжди** — і якщо в Кореї дешевше, і якщо дорожче (податки — по P, за авто платиш фактичну корейську ціну).

| Рік | P (USD) | Вік (2025−рік) |
|-----|---------|-----|
| 2019 | 25 000 | 6 |
| 2020 | 36 000 | 5 |
| 2021 | 40 500 | 4 |
| 2022 | 43 000 | 3 |

**Таблиця вище — для X5.** У X6 своя, вища (нове авто дорожче). Поки її немає, X6 рахуємо
по X5-таблиці й позначаємо `priceEstimated: true` → на сайті «≈». Реально +$1000…2000.

```
car   = korea_price_만 * 10000 / KRW           # KRW=1372 ₩/$
duty  = 0.10 * P                                # мито
excise= rate * 3.0 * age * 1.1648              # дизель rate=75 €/л; митниця бере 2993 см³ для всіх; EUR/USD=1.1648
vat   = 0.20 * (P + duty + excise)
розмитнення = duty + excise + vat
pens  = 0.05 * P                                # пенсійний збір
oblik = pens + 124                              # $124 = сертифікація $79 + реєстрація $45
ship  = 3700                                    # логістика
svc   = 1000                                    # послуги
ПІД КЛЮЧ = car + розмитнення + oblik + ship + svc
```
Округлення: `int(x+0.5)`. Формула звірена з усіма 8 скрінами (±$1–2).
**ХИБНА стара версія** (не використовувати): EUR-база 21500/31000/35000/39000 €, вік=2026−рік.

---

## Encar API (endpoints)
Заголовки: `User-Agent: Mozilla/5.0…` + `Referer: https://fem.encar.com/`. Проксі egress періодично віддає **HTTP 407** — рятують ретраї в циклі.

- **Пошук** (працює на 2026-09-02): `https://api.encar.com/search/car/list/general?count=true&q=<QUERY>&sr=|ModifiedDate|<offset>|20`, пагінація по 20.
  Синтаксис `q`: `(And.Hidden.N._.(C.CarType.A._.(C.Manufacturer.BMW._.(C.ModelGroup.X6._.Model.X6 (G06).))))` — URL-encoded.
  **Увага:** старий `Hybrid.N` / `CarType.N` і шлях `/list/premium` тепер віддають **404**. Правильні — `Hidden.N`, `CarType.A`, `/list/general`.
  Відповідь: `{Count, SearchResults[]}`, поля `Id, Model, Badge, FuelType, Year, FormYear, Mileage, Price, SellType, Photos[]`.
  Поле `Year` = YYYYMM (виготовлення) → **рік виготовлення = Year // 100** (для митниці; відрізняється від `FormYear` = модельний рік). `SellType`: 일반=звичайний, 리스=лізинг, 렌트=оренда.
  Бейджі дизельних X6 G06: `xDrive30d M 스포츠` (наш), `xDrive40d M 스포츠…`, `M50d`, `xDrive30d xLine` — з них беремо лише перший.
- Деталь: `https://api.encar.com/v1/readside/vehicle/{listingId}` → `vehicleId`, `vin`, `spec.colorName` (кузов, корейською), `spec.customColor`, `options.standard` (числовий каталог Encar — акустичного скла там НЕМАЄ).
- Історія: `…/record/vehicle/{vehicleId}/open` → `myAccidentCnt/myAccidentCost`, `otherAccidentCnt/Cost`, `ownerChangeCnt`, `totalLoss`, `flood`.
- **Інспекція: `…/inspection/vehicle/{vehicleId}` — найтвердіше джерело в проєкті.**
  Державний звіт про стан (성능점검기록부), а не слова продавця. Модуль `tools/inspection.py`
  (НЕ `inspect.py` — те ім'я перекриває стандартний модуль Python). Дає те, чого немає більш
  ніде:
  | Поле | Що це |
  |------|-------|
  | `master.detail.usageChangeTypes` | **прокат / таксі / службове** у минулому — в оголошенні цього немає взагалі |
  | `master.accdient` | ДТП **силових елементів** (≠ виплати страховика) |
  | `master.simpleRepair` | просте кузовне |
  | `outers[]` | панель-за-панеллю: `교환(교체)` заміна, `판금/용접` зварювання, + ранг деталі |
  | `master.detail.mileage` + `issueDate` | пробіг на дату інспекції → ловить скручений |
  | `master.detail.comments` | вільний текст інспектора; там буває «미확정보험이력있음» — виплата ще не проведена, і в `record` API її ще нема |
  | `master.detail.vin` | ще одне джерело VIN |
  | `seriousTypes`, `waterlog`, `tuning`, `recall` | потоп / пожежа / списання, тюнінг, відкликання |
  **Ранг деталі критичний:** `RANK_ONE/TWO` — зовнішні накладні (капот, крила, двері,
  кришка багажника), у Кореї це дрібний ремонт. `RANK_A/B/C` — каркас; саме через нього
  `accdient` стає True.
  **⚠️ `vehicleId` ≠ `listingId`** у більшості лотів (21 з 45) — брати з detail API
  (`vehicleId` або `manage.dummyVehicleId`), інакше приходить 404.
- Діагностика: `…/diagnosis/vehicle/{vehicleId}` → панелі (`resultCode` REPLACEMENT = заміна).
  Це платний продукт Encar, є лише у 12 з 45 лотів — на нього не спиратись.
- **VIN найпростіше брати з detail API** (`vehicle/{listingId}` → `vin`). Але VIN може **зникати** з відповіді (у 42445415 був, потім пропав) — не перетирати вже збережений VIN значенням `null`.
- **Чи продано** (перевіряти перед показом списку іншим):
  - `HTTP 404` — оголошення знято;
  - `advertisement.salesStatus == "CONTRACT"` — продано, контракт підписано (`status` при цьому все ще `ADVERTISE`!);
  - `advertisement.price == 9999` — ціну приховано, майже завжди супроводжує продаж;
  - `advertisement.price` — актуальна ціна в 만원; звідси перераховуємо ціну під ключ.
- **Фото:** `photos[]` у detail API → `{code, path, type}`, `type` = `OUTER` / `INNER` / `OPTION`. Беремо OUTER+INNER, **сортуємо за номером кадру** — `_001` це головний ракурс, порядок у відповіді довільний.
  URL: `https://ci.encar.com<path>?impolicy=heightRate&rh=<H>&cw=<W>&ch=<H>&cg=Center`. Робочі розміри 16:9: `280×158` (мініатюра, ~16 КБ), `640×360`, `800×450` (~92 КБ). Без policy — оригінал 640×360.
- Корейські кольори кузова: 흰색=білий, 검정색=чорний, 청색=синій (на G05 = Phytonic Blue), 쥐색=сірий, 진주색=перловий, **연금색=бронзовий** (це опис відтінку, а не назва фарби). `customColor`: 카본블랙=Carbon Black, 아크틱그레이=Arctic Grey, 네이비=Navy, **맨하탄=Manhattan**, 미네랄화이트=Mineral White.
**Назву фарби часто пише продавець в описі** — там вона точніша за `colorName`: у `41578153` Encar дав 연금색, а продавець «맨하탄», тобто Manhattan. Так само `41032632`.

---

## Точна комплектація за VIN (BMW build sheet)
Декодер: **mdecoder.com/decode/{VIN}** — повертає назви фарби/оббивки + коди BMW і **повний список SA-опцій** (S-коди).
- Технічно: curl з **cookie jar** (`-b/-c`) + **полінг** — перша відповідь порожня оболонка «Data will be available in 30 seconds», дані на 2-му запиті (~30с).
- **Добовий ліміт на IP** («You have reached a daily limit… try again tomorrow», title «Limited access»); свіжий jar НЕ обходить. Скидається наступної доби.
- Батч 23 VIN вичерпує квоту → декодуй партіями або проси користувача вставити білд-лист вручну (він це вміє).
- **PDF-експорт mdecoder**: користувач може віддати білд-лист як PDF (`~/Downloads/BMW G05 X5 … - <VIN>.pdf`) — парсити `pdftotext -layout`. У PDF назви опцій у Title Case і бувають OCR-огріхи (`Runhat`=Runflat, `Country-Specihc`=Country-Specific, `Runiat`=runflat), але коди `S…` завжди коректні. У PDF частіше присутні коди фарби/оббивки (`S416`, `SMCSW`) і реальна потужність.
- **bimmer.work ПРАЦЮЄ і дає повний білд-лист — перевірено 2026-09-02 на `42655967`.**
  Користувач вставив результат руками, і там усе, що треба: фарба з кодом (416
  Carbonschwarz), **оббивка з кодом (`VAHF` Leder Merino Coffee)**, повний список SA-кодів
  (62 шт.), потужність (210 кВт / 650 Нм), точна дата випуску, завод (Spartanburg) і
  повний код двигуна (`B57D30O2`). Формат: `код ⏎ код ⏎ English name ⏎ German name`.
  **Розшифровки якісніші за mdecoder:** те, що mdecoder віддавав як «Dummy-SALAPA»,
  насправді `S8KH` = інтервал заміни оливи 20 000 км / 18 міс, а `S925` = пакет захисту
  при транспортуванні. Обидва виправлені в `options.js`.
- **✅ ОФІЦІЙНИЙ API bimmer.work — тепер це головний декодер (2026-09-02).**
  Користувач оформив API Trial на свій акаунт (`volodymyr.babak@gmail.com`), **дійсний до
  2026-09-03 15:13 GMT+1**. Далі — платно (API 1 тиждень 79,99 €), тож **поки токен живий,
  декодувати треба все, що можна**.
  ```
  GET https://bimmer.work/api/v1/vin/<VIN>/?token=<TOKEN>
  ```
  Відповідь — список з одного об'єкта: `status` = `OKAY` / `IN PROGRESS` / `FAILED` /
  `ERROR VIN`; поля `model, ecode, type, chassis, market, engine, color, upholstery, date,
  production, options{код:{en,de}}, images{exterior,interior}`. **Перший запит на «холодний»
  VIN віддає `IN PROGRESS`** — треба полінг (у нас 15 с × до 10 спроб); кешовані VIN
  відповідають одразу. Ліміт по кількості не заявлений («fair use»), **429 не траплялось**
  за ~25 запитів; 407 від нашої проксі — буває, лікується ретраєм.
  **Інструмент: `tools/bimmer.py`.**
  ```
  BIMMER_TOKEN=… python3 tools/bimmer.py            # усі недекодовані з VIN → пише в data/
  BIMMER_TOKEN=… python3 tools/bimmer.py <VIN> …     # лише показати, без запису
  BIMMER_RAW=<dir>                                   # додатково зберегти сирий JSON
  ```
  **Токен у репозиторій не пишемо** — лише через оточення. Скрипт кладе в
  `data/cars/<id>.json` те саме, що й ручна вставка (`prodDate, engine, power, engineNote,
  exterior, interior, options, keyFeatures, decodeSource`), формулювання опцій бере з уже
  наявних у проєкті, щоб не розповзались, і **не чіпає `sellerText` / `sellerFacts` /
  `airSeller`**. Салони під виключення (cognac / айворі / Tacora) він лише **позначає**
  у звіті — прибирає авто зі списку людина.
  **API точний — перевірено на трьох уже відомих білд-листах:** `WBAJU8101M9H27574` →
  Vernasca Black, `WBACV6107KLK23778` і `WBACV6100L9D36425` → Vernasca Cognac. Тобто
  «усі кавові» в результатах — не баг парсера.
  **Бонус — рендери заводської конфігурації, вже на сайті.** `images.exterior` /
  `images.interior` віддають PNG 1280×768 без вотермарки (токен додавати в кінець URL,
  ~0,4–0,9 МБ). `python3 tools/bimmer.py --images` качає їх для всіх авто з VIN, ужимає
  ImageMagick'ом до 1000 px JPEG на білому (~45 / ~70 КБ) у `assets/renders/<VIN>-ext.jpg`
  і `-int.jpg`, а шляхи кладе в `data/cars/<id>.json → renders`. Знято для всіх 34 —
  разом 4,1 МБ. **Це найкраща відповідь на питання «кава чи cognac»:** рендер показує
  саму заводську оббивку, а не фото в жовтому світлі салону дилера.
- **Технічні деталі bimmer.work без API (як було до токена).** Сайт
  сам заявляє «comprehensive options list + configuration images + 360° views», тобто
  опції там мають бути. Але **безкоштовний ліміт прив'язаний до IP і на нашій egress-адресі
  вже вичерпаний**: `/429/ Too Many Requests` прилітає на першу ж спробу (ймовірно, IP
  спільна — Encar із неї періодично віддає 407). Технічно: форма на `/` постить
  `POST /query.php` з полями `vin` + `recaptcha_token` (invisible reCAPTCHA Enterprise);
  результат **не GET-адресований** — `/vin/…`, `/decode/…`, `/{VIN}` віддають 404,
  `query.php` напряму — 403. Заголовка `Retry-After` немає.
  Далі можливі два шляхи: **реєстрація** на `/my/register.php` (email + пароль, потрібна
  згода користувача) або **користувач сам робить запит у браузері й віддає результат —
  так і зроблено для `42655967`, спрацювало добре**.
  **Бонус, якого немає в mdecoder:** 360°-зображення заводської конфігурації — ними можна
  було б закривати питання cognac / кава, яке ні фото з оголошення, ні метрика не беруть.
- Альтернативи vindecoderz, bmw.plus, NHTSA назв оббивки/опцій **не дають**.

### Пневмопідвіска — окремий критерій (рішення 2026-09-02)
**M Sport пневмо НЕ гарантує.** У частині авто стоїть `S2VF` Adaptive M chassis
(адаптивні амортизатори на звичайних пружинах) замість `S2VR`. **Ретрофіт нереальний:**
стійки обох осей + компресор + ресивер + датчики висоти + VDM + прошивка VO ≈ $6000–10 000,
і роблять це вкрай рідко — тобто це властивість авто назавжди, а не опція, яку доберуть.
Тому пневмо винесено в окремий фільтр на головній, а не тільки в рядок опцій.

**Чому це важливо саме нам:** профіль їзди — виїзди по області 100–200 км і Карпати,
плюс 20-дюймові runflat на 2,2-тонному кузові. Справедлива знижка за відсутність пневмо
≈ **$1500–2500** (різниця в ліквідності на перепродажі мінус економія на ремонті:
пневмостійки $900–1400 спереду, подушка $400–700, компресор $500–900 — але й EDC-стійки
без пневмо не безсмертні, $500–800). У списку авто без пневмо цієї знижки **не дають** —
два найдешевші лоти пневмо мають.

**Опис оголошення — безкоштовне й точне джерело.** `tools/seller.py::air_suspension()`
дає `True` / `False` / `None`. Це **єдиний виняток** із правила «полярність руками», бо
заперечення стоїть впритул до згадки: `에어서스 없는차량` = немає, `에어 서스팬션 적용` = є.
Вікно — 10 знаків після згадки (корейське заперечення) і 12 перед (англійське `no/not/
without`); ширше вікно ловить 무사고 з наступного речення, а англійське заперечення після
згадки ловить `에어서스펜션, no paint` і дає хибний мінус.
**Збіг з білд-листом — 3 із 3:** `41032632` і `41142979` (опис «є» → `S2VR`), `42468210`
(опис «немає» → `S2VF`). **`None` ≠ «немає»:** у `42445415` і `42405951` пневмо немає за
VIN, а в описі про неї ні слова. Згадка є лише у 6 з 39 описів.
У даних: `airSeller: true|false` у `data/cars/<id>.json`; `sync_index.py` переносить його
в індекс **лише для недекодованих** — щойно є білд-лист, прапорець зникає, щоб не було
двох відповідей на одне питання (той самий принцип, що в `interiorUnverified`).

### ⛔ Каталог опцій Encar (`options.standard`) — тупик, перевірено 2026-09-02
Числовий каталог **не годиться як джерело комплектації**: з 56 кодів, що трапляються
в наших 39 лотах, **47 присутні в кожному авто** (подушки, ABS, склопідйомники). Варіюються
лише `002`, `004`, `034`, `062`, `077`, `078`, `080`, `092`.
- `002` виглядало як пневмо: є в **12 з 12** декодованих із `S2VR`. Але воно є і в
  `42405951`, де за білд-листом `S2VF` без пневмо → **хибний плюс**. Каталог заповнює
  дилер руками, тож як джерело правди не годиться. Відсутність `002` поки надійніша
  (0 з 12 авто з пневмо його не мають), і без нього стоять 5 недекодованих лотів:
  `42627728`, `42257052`, `40966656`, `42415471`, `42349824` — **підозра на відсутність
  пневмо**, перевіряти за VIN. У дані це НЕ пишемо.
- `034` і `077` дали 1:1 із вентиляцією сидінь, але на 15 авто там лише один негативний
  приклад — доказова сила близька до нуля.
Словника назв кодів немає: `/v1/readside/code/options`, `/common/options`, `/options`
і `vehicle/{id}/options` віддають **404**.

### Ключові S-коди (глосарій)
| Код | Що це |
|-----|-------|
| S2VR | Двовісна пневмопідвіска (Two-axle air suspension) |
| S5AZ | BMW Laserlight (лазерні фари) |
| S552 | Adaptive LED (не лазер) |
| S323 | Soft-Close-Automatic двері |
| S453 | Вентиляція передніх сидінь (climate-controlled) |
| S4HA | Підігрів сидінь пер.+задн. |
| S456 | Комфортні сидіння з памʼяттю · S481 спорт-сидіння · S488 люмбар |
| S4NB | 4-зонний клімат |
| S402 | Панорама (S407 = Sky Lounge, з підсвіткою) |
| S610 | Head-up Display |
| S688 | Harman/Kardon · S6AR = Bowers & Wilkins · S676 = базове HiFi |
| S1MA | M Sport вихлоп |
| S1CE | 48V mild-hybrid (recuperation) — з **модельного** року 2021, мотор B57**P** (проти B57**O** раніше). Увага: модельний рік 2021 починається з випуску **серпня 2020**, тож 48V буває і на авто 2020 року виготовлення (`41558094`, випуск 19.08.2020) |
| B57D30O0 / B57D30O2 | **Повні коди двигуна з bimmer.work.** `B57D30O0` (2019–20) = 195 кВт / 620 Нм; `B57D30O2` (модельний рік 2021+, з 48V) = 210 кВт / 650 Нм — трапляється вже на випуску серпня 2020. Скорочення B57O/B57P нижче — з поля `spec` в Encar, це те саме |
| B57O / B57P | **Потужність різна:** B57O (2019–20) = **195 кВт / 265 к.с.**; B57P (2021+, з 48V MHEV) = **210 кВт / 286 к.с.** + електробуст. Тобто 2021+ ще й помітно потужніший |
| **S3KA** | **Acoustic glass** (акустичне багатошарове скло на 4 бічні двері) |
| **S4HB** | **Heat Comfort — підігрів керма + підлокітників.** Саме він дає підігрів керма, і він є в усіх 8 декодованих |
| S248 | Підігрів керма ОКРЕМОЮ опцією. Пакет `S4HB` його перекриває, тому разом вони не зустрічаються: `S248` відсутній ≠ керма без підігріву — спершу дивитись на `S4HB` |
| S417 | Задні шторки · S3AC фаркоп · S6UN Night Vision · S2VH Integral Active Steering (кермо задньої осі) |
| **S2VF** | **Adaptive M chassis** — адаптивні амортизатори + звичайні пружини. **Це НЕ пневмо!** Пневмо = `S2VR`; якщо в білдлісті є S2VF без S2VR → пневмопідвіски немає. Трапляється частіше, ніж здавалось — див. розділ «Пневмопідвіска» вище |
| S6F1 | Bowers & Wilkins High End (те саме, що S6AR у старіших списках) — вище за S688 H/K |
| S4M5 | Individual leather-covered dashboard (шкіряна панель приладів; проти базового `S4AW` Sensatec) |
| `SA96` / `SMCSW` | mdecoder іноді видає **коди фарби й оббивки** прямо в «Other equipment» з префіксом `SA…`/`SM…` (тут A96 = Mineral White, MCSW = Vernasca Black) — звідти брати коди для картки |

### ⚠️ Пастка: пакет перекриває окрему опцію
Якщо опція входить у пакет, BMW **не виписує її окремим кодом**. Тому «коду немає»
≠ «функції немає». Перевірені випадки:

| Що шукаємо | Код, який реально стоїть | Окремий код, якого НЕ буде |
|---|---|---|
| підігрів керма | `S4HB` Heat Comfort (8/8) | `S248` (0/8) |
| adaptive cruise control | `S5AU` Driving Assistant Professional (8/8) — ACC це його основа | окремого коду ACC немає |
| памʼять сидінь | `S456` Comfort seat with memory (7/8) | `S459` поруч не з'явиться |
| клімат | `S4NB` 4 зони (8/8) | 2-зонного коду не буде |

### Є в УСІХ 8 декодованих — але важливі ПОКАЗУЄМО
`S1MA` M-вихлоп · `S402` панорама · `S610` HUD · `S4NB` 4-зона · `S4HA` підігрів сидінь
пер.+задн. · `S4HB` підігрів керма · `S5AU` DAP (=ACC) · `S5DN` Parking Assistant Plus ·
`S6U8` жести · `S2NH` гальма M Sport · `S322` Comfort Access · `S3DS` Display Key ·
`S4UR` Ambient · `S4A2` кришталевий iDrive.

**Рішення 2026-09-02: важливі з них лишаються в рядку опцій, хоч і 8/8.** «Є в усіх» —
це властивість вибірки з восьми авто, а не гарантія G05/G06. Якби ми їх не показували,
нове авто **без** ACC чи підігріву керма виглядало б так само, як з ними, і ми б
дорисували йому опцію в уяві. Тому в рядок додано `acc, park, comfort, seatheat,
wheelheat, climate4, exhaust`. Не показуємо дрібне й косметичне: жести `S6U8`,
Display Key `S3DS`, Ambient `S4UR`, кришталевий селектор `S4A2`, гальма `S2NH`,
а також **HUD `S610` — за рішенням користувача 2026-09-02: йому не важливий**
(у білд-листі на сторінці авто він, звісно, лишається).

### Матеріал салону
У всіх 8 декодованих — **шкіра Vernasca** (`MCSW` black / `MCHF` coffee). Merino і Nappa
(вищі тири, BMW Individual) поки не траплялись — якщо з'являться, це буде видно
в назві салону просто як інша перша частина («Merino …» замість «Vernasca …»).
`S4AW` Sensatec — це **шкірозамінник на панелі приладів**, не сидіння; замість нього
`S4M5` Individual шкіряна панель — у `41517612` і `42287839` (2 з 9, рідкість).

**Sky Lounge `S407` — 0/8:** у всіх звичайна панорама `S402`. Слот `roof` покаже
«Sky Lounge», якщо колись знайдеться.

**Акустичне скло `S3KA`:** тільки у VIN-білдлісті (шукати `3KA`); в Encar-каталозі його немає. Європейська опція, у США не пропонували, на корейський ринок йшла рідко → більшість авто без неї. Не плутати з `S4A2 Glass application Crafted Clarity` (кришталевий селектор iDrive).

### Кольори салону (Vernasca на G05/G06)
Black **(ЛИШАТИ)**, **Coffee (кавовий `MCHF` → ЛИШАТИ)**, **Cognac (рудий `MCRI` → ПРИБИРАТИ)**, **Ivory White (айворі → ПРИБИРАТИ)**, **Tacora Red (червоний → ПРИБИРАТИ)**.
**Крім Vernasca буває `Leather Merino` (Extended Merino)** — вищий тир шкіри (BMW Individual).
Знайдений код: **`VAHF` = Leder Merino Coffee** (`42655967`) — кавовий, тобто критерій проходить. Знайдений колір **Tartufo** — трюфельно-коричневий; це **не** cognac/рудий і не айворі, тому проходить критерій «чорний/кавовий». Приклад: №17 `41815032`.
**Фото-класифікація ненадійна** — VIN авторитетний. Приклад: `42092261` фото показало «рудий», VIN = Vernasca Coffee → повернуто в список.

---

## Профіль їзди користувача (для рекомендацій)
Зараз місто; **через ~2 роки** переїзд у ЖК за київським міжріччям → регулярний комют **20–30 км в один бік**; **раз на 1–2 тижні** 100–200 км по області; **раз на 1–3 місяці** Львів/Карпати. Разом **≈ 18–20 тис. км/рік**. Це трасовий/дальній профіль → **дизель ідеальний** (комют 20–30 км достатній для DPF, не «дрібне місто»).

## TCO-висновки (4 роки × ~19k км, Україна, USD)
- **Дизель дешевший за PHEV (45e)** на ~$4–5k/4р (батарейний ризик + залежність від зарядки + гірша амортизація; митних пільг PHEV не має — рахується як бензин).
- **Дизель дешевший за бензин (40i)** на ~$5–6k/4р (пальне +$3–4k: 40i ~11,5 л/100 проти дизеля ~8; + гірша амортизація/ліквідність в Україні).
- Рухомі витрати між двома однаковими дизелями скорочуються → різниця = **ціна входу + пробіг + стан + залишкова вартість**.

## Ретрофіт-вартість (Україна, орієнтовно)
- **Soft-Close `S323`:** ~$1 000–1 600 (aftermarket-кит) / OEM ~$3 500–5 000. **Варто.**
- **Підігрів керма:** ретрофіт не потрібен — його дає пакет `S4HB`, який є в усіх декодованих авто добірки. (Окрема опція `S248` коштувала б ~$200–400.)
- **Лазер `S5AZ`:** ~$5 000–10 000 — **не варто** (адаптивний LED і так добрий).
- **Масаж:** потребує інших сидінь + модуль → ~$3–6k+, практично не роблять. Зі слотів рядка опцій прибраний (див. нижче) — у добірці його не траплялось.
- **Bowers & Wilkins:** ~$3–5k, недоцільно.
- **Акустичне скло:** OEM-нове ~$1 000–1 800; б/в з розборки ~$400–600 (перевіряти маркування `//`/«Acoustic» на кутку скла; донор має бути з S3KA — рідкість). Альтернатива для тиші: **шумоізоляція дверей/арок ~$300–700** — часто ефективніше.

---

## Поточний шортлист — джерело правди `data/cars.json`
Таблицю тут **не тримаємо** (десятки рядків, швидко застаріває). Актуальний список — у `data/cars.json`,
відсортований за ціною під ключ; `meta.count` і `meta.decodedCount` показують стан.

Станом на 2026-09-02: **39 авто** — 20 X5 (G05) + 19 X6 (G06), **декодовано 34**
(усі, у кого є VIN). Без білд-листа лишились **5 авто, у яких VIN не дає ніхто**:
`42554478` ($58 161), `42585873` ($60 353), `42595623` ($60 426), `42094631` ($65 999),
`41616675` ($69 053) — перевірено detail API, звіт інспекції, `record` і текст оголошення.
`watch.py` підхопить VIN, якщо він з'явиться.

### Масовий декод через API bimmer.work (2026-09-02)
Одним проходом `tools/bimmer.py` декодовано **20 авто** (15 з наявним VIN + 5, яким VIN
відновили — див. нижче). Витрачено ~25 запитів, ліміту не впіймали.

**⚠️ Салон: усі 20 виявились кавовими — жодного виключення.** Разом по вибірці:
**Vernasca Coffee 20 · Merino Coffee 9 · Vernasca Black 5**. Cognac / айворі / Tacora
серед недекодованих не було. Це перевірено проти API на трьох відомих білд-листах
(див. розділ про API), тож це не хиба парсера, а властивість вибірки: чорні салони ми
декодували раніше, а корейські X5/X6 M Sport масово брали кавовий.
Два «коричневих» описи закрились кавою: `41578153` і `41456161` — обидва `MCHF`.

**Як добули VIN, якого не було в оголошенні (5 авто).** Порядок дешевизни:
1. **`master.detail.vin` у звіті інспекції** — дав повний VIN для `41701983`, `42646084`,
   `42585911`, `42476461`. У detail API його там немає взагалі. Це другий за надійністю
   джерело VIN після detail.
2. **⚠️ Encar переставив знаки місцями.** `41413915`: detail API віддає
   `WBAGT810MX9E57421`, API bimmer каже `ERROR VIN`. Контрольний знак (9-та позиція) і код
   року (10-та) **переставлені**: правильно `WBAGT810XM9E57421` (X = контрольний, M = 2021).
   Перевірка — стандартна формула контрольного знака VIN; вона ж підтвердила решту VIN
   у проєкті. **Завжди рахувати контрольний знак, перш ніж вважати VIN битим.**
3. **Хвіст VIN + обчислений контрольний знак.** `42627728`: звіт інспекції дав лише
   `9H56351`. Префікс для X5 G05 30d 2021+ — `WBAJU810`, код року `M`; контрольний знак
   обчислено → **5**, тобто `WBAJU8105M9H56351`. bimmer.work підтвердив: X5 xDrive30d G05,
   випуск 11.06.2021 — збігається з лотом. **Префікси: X5 2019–20 `WBACV610`, X5 2021+
   `WBAJU810`, X6 `WBAGT810` (2020 — `WBAGT210`).**

**⚠️ Рік Encar (연식) — це МОДЕЛЬНИЙ рік, а не календарний рік випуску.** У 8 авто дата
випуску з білд-листа на 1–3 місяці **раніша за рік Encar**: `41701983` (випуск 04.12.2019
проти 연식 2020), `42646084` (12.12.2019/2020), `41368455` (23.11.2020/2021), `41382456`
(09.11.2020/2021), `41413915` (03.10.2020/2021), `41437048` (05.10.2021/2022), `41689680`
(29.10.2021/2022), `42585911` (16.11.2021/2022). Код року у VIN завжди збігався з роком
Encar. **Це відкрите питання по грошах:** наша таблиця `MIN_CUSTOMS_VALUE` бере рік із
Encar, а різниця між сусідніми роками величезна (2019 → $25 000 проти 2020 → $36 000, це
приблизно −$4000 під ключ). Якщо українська митниця візьме **календарний** рік випуску
(а білд-лист — найтвердіший доказ дати), ці 8 авто розмитнюються **дешевше**, ніж показує
сайт. Поки нічого не міняли — потрібне рішення користувача / розрахунок carspy.

**Що видно по вибірці з 34 декодованих:**
| Опція | Скільком є |
|---|---|
| панорама, ACC, паркування, Comfort Access, підігрів сидінь і керма, 4-зона, M-вихлоп | 34/34 |
| вентиляція сидінь `S453` | 31/34 |
| **пневмо `S2VR`** | **25/34** |
| 48V `S1CE` | 24/34 |
| Harman/Kardon `S688` | 19/34 |
| лазер `S5AZ` | 18/34 |
| Soft-Close `S323` | 14/34 |
| адаптивний LED `S552` | 9/34 |
| Bowers & Wilkins `S6F1` | 6/34 |
| Sky Lounge `S407` | 1/34 (`42585911`) |
| **акустичне скло `S3KA`** | **0/34** |
- **Акустичного скла немає ні в кого з 34** — слот `acoustic` у рядку опцій показує лише
  перекреслене. Тримаємо його як доказ, що це рідкість, а не як фільтр.
- **Без пневмо 9 авто** (VIN-перевірено): `42445415`, `42405951`, `42468210`, `42627728`,
  `42257052`, `40966656`, `42415471`, `42349824`, `41456161`.
- **Модельні 2022 — аудіо базове.** У всіх дев'яти авто 2022 року стоїть `S676` HiFi
  замість H/K або B&W. Тобто «дорожче й новіше» ≠ краще оснащене.
- **Дефіцит чипів видно в кодах 2022 року.** `S3D0` = решітку Iconic Glow **не встановлено**
  (5 авто X6 2022), `S6UX` = контролер iDrive без тач-панелі (5 авто), а `S6UY` у
  `41689680` ($69 418) — **сенсорні функції скасовано взагалі**. Для найдорожчого X5 у
  списку це помітний мінус, якого не видно ні в оголошенні, ні на фото.


**Перший декодований X6 — `42405951`** (bimmer.work, вставлено користувачем 2026-09-02):
VIN `WBAGT8108M9H53103`, 2021, Carbonschwarz `416`, оббивка **Vernasca Coffee `MCHF`** —
критерій салону проходить. Особливості: **пневмопідвіски немає** (`S2VF` Adaptive M chassis
без `S2VR`), зате `S6F1` Bowers & Wilkins, `S5AZ` лазер, `S323` Soft-Close, `S453` вентиляція,
`S775` стеля антрацит, `S3MB` Individual Exterior Line. Нові коди в `options.js`:
`S3DN` (решітка Iconic Glow) і `S4KM` (алюміній Mesh Effect темний).

**`42468210` декодовано** (bimmer.work, вставлено користувачем 2026-09-02):
VIN `WBAJU8101M9H27574`, 2021 (випуск 21.05.2021, Spartanburg), фарба **Arktikgrau
Brillanteffekt `C27`** (Encar показував просто «сірий»), оббивка **Vernasca Black `MCSW`**
— критерій проходить. **Слова продавця підтвердились і покращились:** пневмопідвіски
справді немає (`S2VF` Adaptive M chassis без `S2VR`), а аудіо він назвав Harman/Kardon,
хоч насправді стоїть `S6F1` **Bowers & Wilkins Diamond** — тир вище. Ще: `S5AZ` лазер,
`S323` Soft-Close, `S453` вентиляція, `S775` стеля антрацит, `S3MB` Individual Exterior
Line, `S1TC` диски 20". Акустичного скла `S3KA` немає. Новий код в `options.js`: `SC27`.
**Це друге авто підряд без пневмо серед декодованих із bimmer.work** — на корейському
ринку `S2VF` трапляється частіше, ніж здавалось; «M Sport» пневмо не гарантує.

**`41032632` декодовано** (bimmer.work, вставлено користувачем 2026-09-02):
VIN `WBAGT2102L9D05449`, 2020 (випуск 21.05.2020, Spartanburg), фарба **Manhattan
Metallic `C3D`** — тепер із кодом (Encar давав 연금색 «бронзовий», продавець писав
«맨하탄»; білд-лист підтвердив продавця), оббивка **Vernasca Coffee `MCHF`** — критерій
проходить. **Обидва твердження продавця підтвердились точно:** `S2VR` пневмо є,
`S688` Harman/Kardon є. Це **перше з декодованих X6 із пневмопідвіскою**. Двигун
`B57D30O0` без 48V (195 кВт), бо 2020 рік. Немає: `S323` Soft-Close, `S3KA` акустичне
скло. Нових кодів у `options.js` не з'явилось — усі 57 уже були.
**⚠️ Коду фар немає взагалі** — ні `S5AZ` лазера, ні `S552` Adaptive LED, тож слот
`light` на сайті перекреслений. Це не «без фар»: базові LED на G06 M Sport ідуть
стандартно й окремим SA-кодом не виписуються. Перший такий випадок у добірці —
перекреслений `light` читати як «адаптиву/лазера немає», а не як відсутність світла.

**`41558094` декодовано** (bimmer.work, вставлено користувачем 2026-09-02):
VIN `WBAGT8105M9D98682`, фарба **Carbonschwarz `416`**, оббивка **Vernasca Coffee `MCHF`**
— перший із трьох «коричневих» описів **виявився кавою, а не cognac** (продавець не збрехав).
`S2VR` пневмо є, `S688` H/K, `S1CE` 48V. Немає: `S323` Soft-Close, `S3KA` акустичного скла
і взагалі фарного коду (ні `S5AZ`, ні `S552`) — як у `41032632`.
**⚠️ Головне: 48V і 210 кВт бувають на авто 2020 року виготовлення.** Випуск 19.08.2020,
але VIN каже `M` = **модельний** рік 2021, і двигун уже `B57D30O2` з `S1CE`. Правило
«2019–20 = 195 кВт, 2021+ = 210 кВт» діє за **модельним** роком, а Encar показує рік
**виготовлення** — на межі серпня 2020 вони розходяться. Практично: серед 2020-х авто
добірки можуть бути і 195, і 210 кВт, і без білд-листа їх не відрізнити. Глосарій
`S1CE` / `B57D30O2` виправлено.

**`41688103` декодовано** (bimmer.work, вставлено користувачем 2026-09-02):
VIN `WBAGT2108L9D36091`, X6 G06, 2020 (випуск 26.06.2020, Spartanburg), фарба
**Mineral White `A96`** (Encar давав просто «білий»), оббивка **Vernasca Coffee `MCHF`**
— критерій проходить, салон був невідомий узагалі (продавець про нього не писав).
`S2VR` пневмо є, `S688` H/K, `S453` вентиляція, `S1XN` диски 21", `S3DN` решітка
Iconic Glow, `S775` стеля антрацит, `S3MB` Individual Exterior Line, `S4KM` алюміній
Mesh Effect. Немає: `S323` Soft-Close, `S3KA` акустичного скла, `S1CE` 48V (двигун
`B57D30O0`, 195 кВт — тут рік виготовлення і модельний збіглися, VIN каже `L`=2020).
Нових кодів для `options.js` немає.

**⚠️ Фарний код відсутній у X6, але не в X5 — це вже закономірність, а не випадок.**
Третє авто підряд без будь-якого коду фар (`41032632`, `41558094`, `41688103`) — і всі
три X6. Статистика по 15 декодованих: **3 з 5 X6 без адаптиву/лазера**, а серед **10 X5
таких нема ні одного** — кожен має або `S552`, або `S5AZ`. Тобто на корейські X6
адаптивний LED замовляли помітно рідше, і перекреслений слот `light` для X6 — норма,
а не сигнал про дивний лот. На пневмо це **не** поширюється: `S2VR` є в 4 з 5 X6 і в
8 з 10 X5 — тут різниці між моделями немає, тож «скромніша» тільки оптика.

**Що дав звіт інспекції по всіх 45 авто 2026-09-02:**
- **Прокатна історія (렌트) у 4 авто — усі ПРИБРАНІ 2026-09-02** за рішенням користувача:
  `42552103` ($58 603), `42171783` ($62 102 — було декодоване, білд-лист переїхав у
  `data/sold/`), `42478734` ($63 944), `41056198` ($64 541). В оголошеннях про це ні слова.
- `42478734` — **ДТП каркаса** (`accdient=True`): задня панель зварена (RANK_A), кришка
  багажника замінена. Продавець писав лише «단순 판금».
- Заміна накладних деталей у 7 авто: капот (`40966656`, `42257052`), праве переднє крило
  (`41689680`, `42552103`), праві задні двері зварені (`42171783`), права боковина зварена
  (`42257052`), панель радіатора (`42287839`, `42257052`).
- `42257052` — у коментарі інспектора «미확정보험이력있음 … 보험이력(내차피해) 有 1회(미확정)»:
  **виплата є, але ще не проведена**, тому в `record` API нуль. Це другий такий випадок
  після `41689680` — реєстр виплат відстає, і поріг 5 млн ₩ на нього спиратись не може.
- Тюнінг: `42631623`, `41578153` (за коментарем — сабвуфер).
- Звіту немає у 2 авто: `42554478`, `42094631`.

**Що дав розбір усіх 44 описів 2026-09-02** (курований, у `sellerFacts`):
- `41689680` — **найважливіше**: продавець сам пише «보험이력 미확정 570만원정도 휀다교환»,
  тобто страхова історія ще не закрита і йдеться про ≈5,7 млн ₩ на заміну крила. У реєстрі
  Encar цього ще немає → **наш поріг 5 млн ₩ це авто пропустив лише тому, що виплата не
  встигла з'явитись**. Урок: реєстр виплат відстає, опис буває чеснішим.
- `42468210` — **пневмопідвіски немає**, продавець подає це як плюс («не буде дорогої
  поломки»). Авто недекодоване, тож інакше ми б цього не знали.
- `40966656` — міняли капот (продавець: звичайна вм'ятина, є фото) → звідси 4,5 млн ₩.
- `42552103` — **гарантію продовжено до 14.09.2026**, три ключі.
- `41142979` — салон кавовий **Merino** (тир вище за Vernasca) + пневмо + лазер + Soft-Close.
  **Декодовано 2026-09-02 (bimmer.work, VIN `WBAGT810XN9M18391`):** Merino Coffee `VAHF`,
  пневмо `S2VR`, лазер `S5AZ` — підтвердились. А от **Soft-Close і Harman/Kardon продавець
  вигадав**: `S323` у білд-листі немає, аудіо — базове HiFi `S676`. Урок: опис продавця
  добрий для фактів, які легко перевірити (колір, ключі), але список опцій там буває
  прикрашений — арбітр лише білд-лист.
- `42094631` — продавець сам показує єдиний ремонт (кришка багажника, гілка).
- `42203413` — VIN лежав у тексті (`차대번호`); декодували — і **авто вилетіло**: `MCRI` Vernasca Cognac. Ставка на VIN з опису себе виправдала.
- Розбіжність «пише 무사고, а в реєстрі є виплати» — у 9 авто. Це не завжди обман:
  корейське `무사고` = без ремонту силових елементів, дрібні виплати сюди не входять.

**Прибрано за VIN 2026-09-02: `42244757`** ($55 246, 74 730 км — була третя ціна в списку).
Білд-лист із bimmer.work показав `MCRI` Vernasca **Cognac**, хоч продавець писав «브라운시트».
Білд-лист збережено в `data/sold/`, VIN — у `interiorRejected`.
**З трьох авто з неоднозначним «коричневим» перше перевірено:** `41558094` ($61 917) —
за VIN **Vernasca Coffee `MCHF`**, лишається. Чекають перевірки `41578153` ($67 457)
і `41456161` ($69 609).

**Прибрано за VIN 2026-09-02: `42203413`** ($60 459, 79 541 км, 11-й у списку) — другий
Cognac `MCRI`. Білд-лист із bimmer.work: VIN `WBACV6100L9D36425`, 2020 (випуск 26.06.2020,
Spartanburg), фарба **Arctic Grey `C27`** (Encar давав 쥐색 «сірий»), пневмо `S2VR` є,
`S688` H/K, `S552` адаптивний LED без лазера, `S775` стеля антрацит, `S3MB` Individual
Exterior Line, `S4KK` алюміній Tetragon. Білд-лист у `data/sold/`, VIN — у `interiorRejected`.
**Два висновки:**
1. **VIN з опису відпрацював.** Це те саме авто, VIN якого Encar API не віддає — знайшли
   в тексті за міткою `차대번호`. Збіг код року (`L`=2020) і дати випуску з лотом підтвердив,
   що VIN справді цього авто, а не скопійований з іншого оголошення. Одне декодування
   прибрало кандидата з середини списку.
2. **⚠️ «Опис мовчить про салон» — це не «салон нормальний».** У `42203413` продавець
   узагалі не згадав оббивку (тільки кузов 쥐색), тому `trim.py` не мав за що зачепитись,
   і авто стояло в списку як «салон невідомий» — а виявився Cognac. Тобто **cognac живе
   не лише серед «коричневих» описів**, а й серед тих 21 авто, де про салон невідомо
   нічого. Пріоритет декодування: спершу три «коричневих», далі — найдешевші з невідомих
   (`42595623` $60 426, `42627728` $62 248, `41701983` $62 500).

**Прибрано за описом продавця 2026-09-02 (2 X5).** `42660021` ($55 246, 41 921 км —
було третьою ціною при найменшому пробігу) — `꼬냑 시트`, cognac; `42348688` ($62 532)
— `베이지시트`, айворі (фото підтвердило). Обидва знайдені щойно, коли навчилися читати
`contents.text`; жодне не було декодоване.

**Прибрано за кольором салону з фото 2026-09-02 (10 X6).** Tacora Red: `42551562`,
`42376797`, `42047529`, `41503700`, `42190131`, `42185232`, `41962836`, `42651049`,
`42569192`. Ivory White: `42340650`. Жодне не було декодоване, тож даних mdecoder
не втрачено; детальні файли видалено, VIN лежать у `watch-state.interiorRejected`.
Найдорожча втрата — `42551562` ($67 350, 31 232 км, найменший пробіг серед X6).

**Прибрано як продані 2026-09-02** (білд-листи переїхали в `data/sold/`, щоб не втратити дефіцитні дані mdecoder):
| Лот | Було під ключ | Чому прибрано |
|-----|---------------|----------------|
| 42543879 | $65 517 | detail API віддає 404 — оголошення знято |
| 42579054 | $63 702 | `salesStatus=CONTRACT` |
| 42422608 | $66 108 | `salesStatus=CONTRACT`, ціну замінено на 9999만 |
| 41815032 | $68 687 | `salesStatus=CONTRACT`; ціна зросла до 6850만 = $71 604, вже понад стелю |

**Формула звірена з реальними цінами.** Корейські ціни з `advertisement.price` дали ті самі
«під ключ», що були в списку, з різницею $2–4 (порядок округлення). Тепер `koreaPriceMan`
зберігається в даних, і `breakdown()` рахує вартість авто з неї, а не як залишок.

**Ціни X6 — оцінка.** Таблиця P звірена скріншотами carspy для X5; для X6 узято ту саму.
Своя таблиця в X6 вища (авто дорожче нове), тож фактично **+$1000…2000** до показаного.
У даних це `priceEstimated: true`, на сайті — «≈» перед сумою. Щоб закріпити, потрібен
**один розрахунок carspy по будь-якому X6** — тоді додати X6-таблицю в `MIN_CUSTOMS_VALUE`.
Чутливість: +10% до P → +$1500 під ключ (d(під ключ)/dP ≈ 0,37).

**X6: історія ДТП відсіює половину.** З 95 X6 30d M Sport, що пройшли ціну/рік/пробіг,
після дедуплікації за VIN лишилось 62, а поріг 5 млн ₩ пройшли **31**. Дублів під різними
listingId у X6 дуже багато (48 унікальних VIN на 80 лотів) — дедуплікація обовʼязкова.

---

## Сайт (ЗРОБЛЕНО, опубліковано)
**https://volodymyr-babak.github.io/bmw-from-korea/** — Pages увімкнено з гілки `main`, корінь репо (`.nojekyll`).
Публікація = звичайний `git push` у `main`; збірки немає, статика.
1. **Головна** `index.html` — список кандидатів із фото, фільтри (модель/рік/пробіг/ціна/**колір кузова**/лише декодовані) і сортування (зокрема за виплатами на ремонт), «драбина» запасу до стелі $70k, виплати за ремонт і кількість власників, рядок ключових опцій (зелена ✔ = є / сіре закреслене = немає).
2. **Сторінка авто** `car.html?id=<listingId>` — галерея фото з оголошення (кузов + салон), розклад платежів під ключ, кольори BMW з кодами, історія, **повна комплектація за VIN** (SA-коди українською, згруповані).
3. Дані — тільки з JSON у `data/`, у розмітці нічого не захардкоджено.

### Файли сайту
```
index.html · car.html
assets/css/site.css      світла/темна тема на CSS-змінних, IBM Plex
assets/js/common.js      breakdown() — формула розмитнення; форматери; KEY_FEATURES; featureState();
                         PAINT_COLORS + paintHex()/swatch() — квадратик фарби; тема
assets/js/options.js     GROUPS + OPTIONS: глосарій SA-кодів українською; groupOptions()
assets/js/index.js       головна
assets/js/car.js         сторінка авто
tools/sync_index.py      переносить VIN/кольори/keyFeatures з data/cars/*.json у data/cars.json
tools/bimmer.py          декодер VIN через API bimmer.work (+ --images: рендери в assets/renders/)
assets/renders/          <VIN>-ext.jpg / <VIN>-int.jpg — заводські рендери, 1000 px, ~4 МБ на 34 авто
```
**Декодування — порядок вибору джерела:**
1. **API bimmer.work** (`BIMMER_TOKEN=… python3 tools/bimmer.py`) — поки токен дійсний
   (Trial до 2026-09-03 15:13 GMT+1). Робить усе саме, включно з `keyFeatures`.
2. **Користувач вставляє результат із браузера bimmer.work** — коли токен скінчився;
   далі руками в `data/cars/<id>.json` → `sync_index.py` → нові коди в `options.js`.
3. **mdecoder.com** — остання опція, добовий ліміт на IP.
У детальному файлі завжди лишати `decodeSource`, щоб було видно, звідки взято білд-лист.

**Робочий цикл додавання авто:** дописати `options`/`keyFeatures`/кольори в наявний `data/cars/<id>.json` → `python3 tools/sync_index.py` →
новий S-код (якщо є) дописати в `assets/js/options.js` → `git push`. Скрипт сам виставляє
`decoded: true` і `meta.decodedCount`; неперекладений код не ламає сторінку, а падає в групу «Інше» англійською.
Локально дивитись через `python3 -m http.server` (ES-модулі не працюють з `file://`).

### Структура даних
```
data/
├── cars.json           # індекс: meta + масив авто (summary для головної)
├── cars/<id>.json      # деталь: фото, історія, koreaPriceMan; де знято білд-лист — ще й options
└── sold/<id>.json      # архів білд-листів проданих авто
```
- `data/cars.json` → джерело для **головної** (список/фільтри/сортування).
- `data/cars/<listingId>.json` → джерело для **сторінки авто**.
- **Детальний файл є в КОЖНОГО авто** (фото й історія доступні через API одразу).
  `decoded: true` означає інше — що в детальному файлі є `options`, тобто знято білд-лист BMW за VIN.
  Саме так це і рахує `tools/sync_index.py`; наявність файлу нічого не означає.
- Поля індексу: `rank, listingId, model, year, mileageKm, koreaPriceMan, priceUSD, priceEstimated?,
  vin, decoded, mark?, exterior?, interior?, interiorSeller?, accident{costKRW,owners}, note?,
  photo, keyFeatures?`.
  `interior` — салон **за VIN** (білд-лист). `interiorUnverified` — непідтверджений салон
  із зазначенням джерела в самому значенні («коричневий / Coffee — з опису», «чорний — з
  фото»). На головній він зі знаком `?`, на сторінці авто — з приміткою «не підтверджено
  за VIN». `sync_index.py` прибирає `interiorUnverified`, щойно з'явився `interior`,
  щоб не було двох відповідей на одне питання.
- `renders` у деталі — `{exterior, interior}` зі шляхами на `assets/renders/<VIN>-*.jpg`
  (рендери заводської конфігурації за VIN, див. `tools/bimmer.py --images`). Панель
  «Заводська конфігурація за VIN» на сторінці авто малює `panelRenders()` у `car.js`
  одразу під галереєю; підпис прямо каже, що це не фото цього авто.
- Поля деталі, що не йдуть в індекс: `sellerText` (сирий текст оголошення, зберігати
  завжди) і `sellerFacts` — список `{kind, text}`, де `kind` = `plus` / `minus` / `info`.
  Рендерить `panelSeller()` у `car.js` окремою широкою панеллю «Що пише продавець»;
  `minus` малюється кольором `--warn`. Для нових авто `watch.py` кладе туди автофакти,
  курувати їх треба руками.
- `photo` — шлях Encar головного кадру (`/carpicture…/…_001.jpg`); розмір підставляє `photoUrl()`.
- `keyFeatures` (виводяться з S-кодів функцією `tools/mdecoder.py::key_features`):
  `air, laser, led, soft, vent, hk, pano, skylounge, acoustic, mhev, acc, park,
  comfort, seatheat, wheelheat, climate4, exhaust` + опційний `bw`.
  **`massage` прибрано 2026-09-02** — код `S4NM` так і не підтвердився на живому
  білд-листі, а масажу не було ні в кого з 8; слот показував лише перекреслене.
- У рядку опцій **15 слотів**, з них три **адаптивні** — показують вищий тир, який реально є:
  | Слот | Логіка |
  |------|--------|
  | `light` | `laser` (S5AZ) → інакше `led` (S552) → інакше перекреслено |
  | `audio` | `bw` (S6F1/S6AR) → інакше `hk` (S688) → інакше перекреслено |
  | `roof` | `skylounge` (S407) → інакше `pano` (S402) → інакше перекреслено |
  Решта слотів прості: `air` (S2VR) · `soft` (S323) · `vent` (S453) · `acoustic` (S3KA) ·
  `mhev` (S1CE) · `acc` (S5AU) · `park` (S5DN) · `comfort` (S322) ·
  `seatheat` (S4HA) · `wheelheat` (S4HB або S248) · `climate4` (S4NB) · `exhaust` (S1MA).
  Логіка — `featureState()` в `assets/js/common.js`, набір — `KEY_FEATURES` там само.
- Декодовані дані — найцінніші, бо **mdecoder має добовий ліміт на IP** (вичерпано 2026-09-02
  на першому ж X6 VIN). **Не втрачати їх**: продані авто йдуть у `data/sold/`, а не видаляються.

### Дизайн (реалізовано)
- IBM Plex: Sans Condensed 600 — шапки, Sans — текст, Mono — усі цифри (ціна, пробіг, VIN, S-коди).
- Палітра в CSS-змінних: папір `#f6f6f3` / чорнило `#14161a` / акцент кобальт `#14559e` / «є» `#1f6b45` / «немає» `#a2a29a`; темна тема — та сама схема через `html[data-theme="dark"]` + `prefers-color-scheme`.
- Тонкі лінійки, нуль радіусів, ліве вирівнювання. Одна «сильна» деталь — драбина ціни $50k→$70k із міткою стелі.
- Для недекодованих авто опції = «Комплектація — уточнюється за VIN».
- **Квадратик фарби** біля назви кольору (головна і сторінка авто) — `swatch()` у
  `common.js`. Відтінки в `PAINT_COLORS` знято **з рендерів bimmer.work** (медіана по
  борту авто), ключі — і код BMW (`416`, `C3D`), і назва в нижньому регістрі, бо в індексі
  лежить лише назва, а в недекодованих — переклад корейської з Encar. **Невідомий відтінок
  штрихуємо** (`.swatch-unknown`), а не вигадуємо колір.
- **Фільтр «Колір кузова»** наповнюється з даних (`fillColors()` в `index.js`): варіанти
  від найчастішого з лічильником, плюс окремий пункт «колір невідомий» для авто без
  `exterior`. Список кольорів у розмітці не хардкодимо — він змінюється разом зі списком.

### Джерела даних (Encar)
- Дані-джерела: `x5n_all.json` (пошук), `x5_full.tsv` (VIN+історія), білд-листи mdecoder.
- Ці файли лежали в тимчасовому scratchpad і можуть зникнути — за потреби перезібрати з Encar API.
- Фільтри й розрахунок митниці, що були в `build_data.py`, тепер живуть у `assets/js/common.js` (`breakdown()`) і в самих даних.

---

## Автоматичний моніторинг (працює з 2026-09-02)
`tools/watch.py` — щогодини з cron (`:17`), встановлюється `tools/install-watch-cron.sh`.
Один прохід: перевірка проданих → пошук нових → декодування VIN → коміт і пуш.

- **Листи** — не з watch.py. Push у `data/last-change.md` запускає
  `.github/workflows/notify.yml`, який створює issue з міткою `encar-watch`.
  **Автор issue мусить бути `github-actions[bot]`**: про власні дії GitHub листів
  не надсилає, тому `gh issue create` під акаунтом власника листа НЕ дасть.
- **Стан** у `data/watch-state.json`: `rejected` (журнал відсіяних із причиною
  і VIN — щоб не перепитувати щогодини; ~104 записи), `mdecoder.quotaExhaustedOn`,
  `mdecoder.failed` (VIN, де парсер не збігся).
- **Дедуплікація без VIN.** Encar інколи не показує VIN, і тоді дубль за VIN не
  ловиться. Запасний критерій (`TWIN_KM = 1000`): той самий рік + та сама ціна
  в 만원 + пробіг зійшовся до 1000 км → це те саме авто. Так відсіявся 42546821
  (63 514 км, 6380만) як близнюк 42574631 (63 515 км, 6380만).
- **Парсер mdecoder написаний наосліп** — добову квоту вичерпали раніше, ніж
  вдалося побачити успішну сторінку. Три стратегії розмітки (таблиця, div-и, `<dl>`),
  валідація «≥25 S-кодів + фарба + оббивка». Якщо не збіглось — статус `unparsed`,
  сирий HTML у `data/mdecoder-raw/<VIN>.html`, у дані **нічого не пишеться**.
  **На першому ж успішному декоді перевірити результат руками.**
- `FEATURE_CODES` у `tools/mdecoder.py` виводить `keyFeatures` із S-кодів.
  Масаж (`S4NM`) звідти **прибрано** — код не підтверджений, а самого масажу
  не було ні в кого з 8 декодованих.
- `data/watch-state.json` → `interiorRejected`: **VIN → колір салону**, відсіяний за фото.
  Перевірка стоїть у `reject_reason()` **до** декодування, і ключ саме VIN, бо той самий
  лот перевиставляють під новим `listingId`, а оббивка в авто не змінюється.
- Маркер «найдешевше» більше **не тримаємо в даних** — його рахує `index.js`
  на рендері. Список оновлюється сам, тому суперлатив у даних швидко стає брехнею.
  У даних лишились тільки кураторські `hero` і `star`.

## Конвенції
- **Мова спілкування — українська.**
- Комміти — Conventional Commits (`feat:`, `fix:`, `docs:`, …).
- Це **особистий** проєкт, окремий від робочого `career-home` (ThingsBoard).
- Ціни/пробіги в даних — станом на перевірку; авто можуть продатися → перевіряти актуальність на Encar.
